export const API_BASE_URL = "http://localhost:8000";

export const DEFAULT_WELCOME_MESSAGE =
  "Hi! I am ready when you are. Send a message to start the conversation.";

export const FALLBACK_REPLY =
  "The backend did not return a reply, so this is a graceful fallback.";
